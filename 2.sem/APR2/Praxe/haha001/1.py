def randomhaha(lst:list):
    result =[]
    for i in lst:
        if i !="":
            result.append(item)

        l=len(result)
        if l==0:
            reise ValueError
        import random
        return result[int(random.random()*l)]
